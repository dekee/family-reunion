Run all tests (backend + frontend) and report results.

1. Run backend tests:
   ```
   cd /Users/derrick/Development/family_reunion && ./gradlew test
   ```
2. Run frontend tests:
   ```
   cd /Users/derrick/Development/family_reunion/frontend && npx vitest run
   ```
3. Report pass/fail counts and any failures.
